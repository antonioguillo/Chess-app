import { IdentifyQuiz } from "@/components/screens/practice/IdentifyQuiz";

export const metadata = {
  title: "Adivina la apertura — Gambito",
};

export default function IdentifyPage() {
  return <IdentifyQuiz />;
}
